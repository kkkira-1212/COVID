# 数据问题修复总结

## 发现的问题

### 1. Week窗口大小不一致 ✅ 已修复
- **旧数据**: 窗口大小 = 8
- **新数据**: 窗口大小 = 4
- **修复**: 已将 `regenerate_data.py` 中的 `window_week` 从 4 改为 8

### 2. 标签生成方式差异

#### 旧6.py方法（6个变量）：
- **方式**: 全局百分位数方法
- **问题**: 所有state使用同一个阈值（如4.0），导致：
  - 某些state正样本过多（如GO有61个）
  - 某些state正样本过少
  - 总正样本数：551 (2.72%)

#### 当前方法：
- **方式**: 按state单独处理的混合方法（spike + trend reversal）
- **优点**: 每个state有自己的阈值，更合理
- **结果**: 总正样本数：351 (1.73%)，减少了200个

#### 旧21.py方法（全部变量）：
- **方式**: 按state单独处理的混合方法（与当前方法相同）
- **结果**: 与当前方法一致

### 3. Week数据聚合逻辑 ✅ 已验证正确

当前逻辑：
1. 标签在日数据上生成（按state单独处理）✓
2. Week聚合时，每个state单独聚合 ✓
3. 使用 `max` 聚合 `outbreak_label`（一周内有任何outbreak就标记）✓

**验证结果**: 聚合逻辑正确，每个state的week数据正确聚合

## 修复方案

### 已完成的修复：
1. ✅ Week窗口大小从4改为8

### 需要确认的问题：

**问题1**: 6个变量时应该使用哪种标签生成方式？
- 选项A: 使用旧6.py的全局方法（虽然不合理，但可能与旧数据一致）
- 选项B: 使用当前方法（按state处理，更合理）
- 选项C: 使用简化的按state百分位数方法

**问题2**: 如果使用旧6.py的全局方法，是否需要：
- 为6个变量创建单独的标签生成函数？
- 还是统一使用按state处理的方法？

## 建议

根据用户的描述"应该是每个state要单独来看"，建议：
1. **统一使用按state处理的标签生成方法**（当前方法）
2. **6个变量和21个变量使用相同的标签生成逻辑**
3. **Week窗口大小统一为8**（已修复）

如果确实需要与旧数据完全一致，可以考虑：
- 为6个变量添加一个选项，使用全局百分位数方法
- 但这不是推荐的做法，因为按state处理更合理

## 最终决定 ✅

**统一使用按state处理的标签生成方法**

- ✅ 6个变量和21个变量都使用相同的标签生成方法（`utils/labels.py`中的`create_outbreak_labels`）
- ✅ Week窗口大小统一为8（已修复）
- ✅ 每个state单独计算阈值和模式，更合理

## 下一步

1. ✅ 确认标签生成方式：统一使用按state处理的方法
2. ✅ Week窗口大小已修复为8
3. 🔄 重新生成数据并验证结果

