# 数据修复说明

## 已完成的修复

### 1. Week窗口大小修复 ✅
- **问题**: Week数据窗口大小从8变成了4
- **修复**: 已将 `regenerate_data.py` 中的 `window_week` 从 4 改为 8
- **状态**: 代码已修复，但需要重新生成数据

### 2. 标签生成方法统一 ✅
- **决定**: 统一使用按state处理的标签生成方法
- **方法**: `utils/labels.py` 中的 `create_outbreak_labels` 函数
- **特点**: 
  - 每个state单独计算阈值和模式
  - 使用混合方法（spike检测 + trend reversal检测）
  - 6个变量和21个变量使用相同的标签生成逻辑

### 3. Week数据聚合逻辑验证 ✅
- **验证结果**: 聚合逻辑正确
- **流程**: 
  1. 标签在日数据上生成（按state单独处理）
  2. Week聚合时，每个state单独聚合
  3. 使用 `max` 聚合 `outbreak_label`（一周内有任何outbreak就标记）

## 需要重新生成数据

由于Week窗口大小已修复，需要重新生成数据：

```bash
# 方法1: 使用脚本重新生成并验证
./scripts/regenerate_and_verify.sh

# 方法2: 手动运行
python3 scripts/regenerate_data.py
python3 scripts/verify_data_consistency.py
```

## 数据配置总结

### Day数据
- **窗口大小**: 14
- **特征数**: 6 或 21
- **标签生成**: 按state处理的混合方法

### Week数据
- **窗口大小**: 8（已修复）
- **特征数**: 6 或 21
- **标签生成**: 按state处理的混合方法
- **聚合方式**: max（一周内有任何outbreak就标记）

## 验证检查点

重新生成数据后，验证脚本会检查：
1. ✓ Day窗口大小 = 14
2. ✓ Week窗口大小 = 8
3. ✓ 标签统计合理
4. ✓ 数据分割正确（70%/10%/20%）
5. ✓ 标准化配置正确
6. ✓ Day-Week映射正确

## 预期结果

重新生成后，Week数据应该：
- 窗口大小 = 8（而不是4）
- 样本数应该与旧数据接近（约2756-2860个样本）
- 正样本比例应该合理（约11-18%）

















